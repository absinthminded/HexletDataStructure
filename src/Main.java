
import java.util.Scanner;
public class Main {
    public static void main(String[] args) {
       // int[] array1 = {1, 8, 1, -10, 3};
        int i = 3;
        int j = 10;

        // Loops.findIndex(array1, i);
        // Loops.replaceElement(array1, i, j);
        // Loops.triangleArray(i);
        //Math.min(-3,0,3);
        //Math.cases(3);
    }
}

